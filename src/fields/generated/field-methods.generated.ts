/**
 * AUTO-GENERATED by scripts/codegen-field-methods.ts — DO NOT EDIT BY HAND.
 *
 * Per-field-type method-name unions, distilled from the engine's column-create
 * API schema (the per-type field-schema definitions). Each
 * `f.<type>` / `input.<type>` constructor types its `methods` against the
 * matching union so only names valid for that field type are accepted (the
 * explicit `{ name, arg }` form remains a universal escape hatch).
 *
 * Regenerate with `npm run codegen:methods` (`-- --refresh` to re-distill upstream).
 */

/** Per-field-type method metadata (method name → colon-form arg type; `bool` = flag, no arg). */
export const FIELD_METHODS: Readonly<Record<string, Readonly<Record<string, string>>>> = {
  "decimal": {
    "max": "int",
    "min": "int"
  },
  "email": {
    "alphaOk": "bool",
    "digitOk": "bool",
    "lower": "bool",
    "max": "int",
    "min": "int",
    "ok": "text",
    "pattern": "text",
    "startsWith": "text",
    "trim": "bool",
    "upper": "bool"
  },
  "int": {
    "max": "int",
    "min": "int"
  },
  "password": {
    "max": "int",
    "min": "int",
    "minAlpha": "int",
    "minDigit": "int",
    "minLowerAlpha": "int",
    "minSymbol": "int",
    "minUpperAlpha": "int",
    "salt": "text"
  },
  "tableRef": {
    "max": "int",
    "min": "int"
  },
  "text": {
    "alphaOk": "bool",
    "digitOk": "bool",
    "lower": "bool",
    "max": "int",
    "min": "int",
    "ok": "text",
    "pattern": "text",
    "startsWith": "text",
    "trim": "bool",
    "upper": "bool"
  },
  "vector": {
    "max": "int",
    "min": "int"
  }
};

/** Field methods valid on `decimal` fields. */
export type DecimalMethod = "max" | "min";

/** Field methods valid on `email` fields. */
export type EmailMethod = "alphaOk" | "digitOk" | "lower" | "max" | "min" | "ok" | "pattern" | "startsWith" | "trim" | "upper";

/** Field methods valid on `int` fields. */
export type IntMethod = "max" | "min";

/** Field methods valid on `password` fields. */
export type PasswordMethod = "max" | "min" | "minAlpha" | "minDigit" | "minLowerAlpha" | "minSymbol" | "minUpperAlpha" | "salt";

/** Field methods valid on `tableRef` fields. */
export type TableRefMethod = "max" | "min";

/** Field methods valid on `text` fields. */
export type TextMethod = "alphaOk" | "digitOk" | "lower" | "max" | "min" | "ok" | "pattern" | "startsWith" | "trim" | "upper";

/** Field methods valid on `vector` fields. */
export type VectorMethod = "max" | "min";
